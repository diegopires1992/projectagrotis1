import styled from "styled-components";
export const ContainerPage = styled.div`
  body {
    margin: 0;
    padding: 0;
  }

  /* Definindo a altura dos elementos para ocupar 100% da viewport */
  html,
  body,
  #root {
    height: 100%;
  }
`;

export const Main = styled.main`
  background-color: #f3f2f1;
  width: auto;
  height: 100vh;
  color: white;
  padding: 1rem;
  text-align: center;
  
`;
